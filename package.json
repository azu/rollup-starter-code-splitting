{
  "name": "rollup-starter-code-splitting",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rollup": "^3.26.0",
    "serve": "^14.2.0"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
